# Pinned: v0.3.0 changed the default Laplacian normalisation (rw -> sym),
# fixed a non-deterministic Lanczos start and removed an eigenvalue filter
# that discarded the near-zero lambda_2 regime. Numbers from 0.2.x are not
# comparable with numbers from 0.3.x.
spectral-trust==0.3.0

torch>=2.1
transformers>=4.44
numpy>=1.24
matplotlib>=3.7
pyarrow>=15  # --attack-source neuralchemy (lecture parquet)
